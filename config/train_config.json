{
    "device": "cuda",
    "lr": 1.6e-4,
    "batch_size": 32,
    "total_steps": 120000,
    "vocab_size": 0,
    "weight_decay": 0.01,
    "log_interval": 100,
    "save_interval": 5000,
    "checkpoint_dir": ".",
    "warmup_steps": 4000
}